# Configuration Azure Storage
# Chaîne de connexion pour Azure Storage (Table Storage + Blob Storage)
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your_storage_account;AccountKey=your_account_key;EndpointSuffix=core.windows.net

# Nom de la table pour les recettes (optionnel, par défaut: recipes)
RECIPES_TABLE_NAME=recipes

# Nom du conteneur pour les images (optionnel, par défaut: recipe-images)
BLOB_CONTAINER_NAME=recipe-images

# Configuration FastAPI
PROJECT_NAME=Myllah Recipe API
VERSION=0.1.0
DESCRIPTION=API pour la gestion des recettes de cuisine

# CORS (optionnel, par défaut: localhost)
ALLOWED_HOSTS=localhost,127.0.0.1
